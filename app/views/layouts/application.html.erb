<!DOCTYPE html>
<html>
  <head>
    <title>HousewifeHelper</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <div class="c_head a_flex">
        <h1>Page</h1>
        <%= link_to "マイページへ", end_users_path, class:"b_link__h" %>
        <%= link_to "献立を決定", root_path, class:"b_link__h" %>
        <%= link_to "メニュー一覧", recipes_path, class:"b_link__h" %>
        <%= link_to "日付を進める", root_path, class:"b_link__h" %>

        <%= form_tag(url: "",:method => 'get') do %>
          <%= text_field_tag :dammy, '', class: 'c_form', placeholder: '飾りです、検索できません。。。' %>
        <% end %>
      </div>
    </header>

    <% if false %>
      <div class="c_box__announce">
        <div class="c_box__announce_inner c_fadein">
          <%= form_with url: "", method: :get do |f| %>
            <% 3.times do %>
              <table class="table c_table__announce">
                <tbody>
                  <tr>
                    <th>
                      <div>
                        <%= image_tag 'cake.jpg', size: '100x100',class:"c_photo__rank" %>
                        <p>メニュー名</p>
                      </div>
                    </th>
                    <td>
                      <div class="c_field__announce">
                        <%= f.select :dammy, ["調理済","未調理"] %>
                      </div>
                    </td>
                  </tr>
                </tbody>
              </table>
            <% end %>
            <div class="a_actions a_pointer c_submit__announce">
              <%= f.submit "確定" %>
            </div>
          <% end %>
        </div>
      </div>
    <% end %>

    <body>
      <%= yield %>
    </body>

    <footer>
      <div class="c_foot">
       <%= link_to "概要", url: "",class:"c_link__f" %>
      </div>
    </footer>
  </body>
</html>
